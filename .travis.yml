sudo: false
language: rust
rust:
  - stable
  - beta
  - nightly

os:
  - linux
dist: xenial

matrix:
  include:
    - os: linux
      dist: xenial
  allow_failures:
    - rust: nightly
  fast_finish: true

before_install:
  - export PATH=$HOME/.local/bin:$PATH
  - pip3 install --user crossbar autobahn

addons:
  apt:
    packages:
    - build-essential
    - libssl-dev
    - libreadline-dev
    - libbz2-dev
    - libsqlite3-dev
    - libncurses5-dev

env:
  global:
    TRAVIS_CARGO_NIGHTLY_FEATURE=""
    RUST_TEST_THREADS=1